# Fake It — 产品需求文档（PRD）

> 2026-07-17 · v2.0 · CBT + 角色视角

---

## 一、产品定位

### 一句话定义

每天遇到跨越时空的角色，让 Ta 以自己的故事和 CBT 思维框架，帮你打破情绪内耗、找到可执行的行动方向。

### 核心理念

你遇到的问题，先人早就遇到过。不是 AI 给你建议，而是透过角色的眼睛看世界——借 Ta 的经历照见自己的路，借 Ta 的思维打破自己的惯性，借 Ta 的语气获得行动的动力。

**Fake it until you make it。** 一次次地「假装」像那个人一样思考，最终把它变成你自己的思维方式。

### 产品气质

- **好玩优先**：角色覆盖现实人物 + 电影/电视剧/游戏/书籍虚构角色
- **情绪价值 + 行动落地**：不只是安慰你，而是帮你打破认知扭曲，给出极小但有效的行动
- **工具型，用完即走**：聚焦「输入问题 → 选角色 → 获得视角 → 行动」

### 与传统 CBT 工具的区别

| 维度 | 传统 CBT 工具 | Fake It |
|------|-------------|---------|
| 语气 | 心理咨询师，专业冷静 | 你敬佩的角色，有温度有故事 |
| 动力来源 | 理性分析 | 角色经历 + 情感共鸣 |
| 接受度 | 需要用户主动学习 CBT | 用户无感知，自然完成 CBT 流程 |
| 好玩度 | 低，像做作业 | 高，像和偶像对话 |

---

## 二、核心功能列表

### 功能 1：问题输入

| 项目 | 说明 |
|------|------|
| 功能 | 用户输入当前遇到的问题/困境 |
| 输入方式 | 文本输入框 |
| 输入示例 | "我该不该辞职创业？" "想提加薪但不敢开口" |
| 字数限制 | 建议不超过 200 字 |
| 后续触发 | 输入完成后，系统生成 5 个角色气泡 + 1 张神秘卡 |

### 功能 2：气泡角色推荐（预生成方案）

| 项目 | 说明 |
|------|------|
| 功能 | 根据用户问题，自动推荐 5 位不同领域、不同时代的角色 |
| 展示形式 | 气泡漂浮，大小 = 匹配度，颜色 = 领域 |
| 角色范围 | 现实人物 + 电影/电视剧/游戏/书籍虚构角色 |
| 领域分类 | 领袖/哲人/探索者/治愈者/反叛者/创作者 |
| 色彩方案 | 多巴胺色系纯色 |
| 用户操作 | 不指定角色，系统自动推荐 |
| 预生成 | 角色推荐与故事预生成一次完成，避免点击气泡时再等待 |

#### 预生成机制

**问题：** LLM 生成多面人生故事需要 2-5 秒，如果用户点击气泡时才生成，体验卡顿。

**方案：** 用户输入问题后，一次 API 调用同时返回 5 个角色 + 每个角色的多面人生故事。

```
用户输入问题
    │
    ▼
显示 loading："正在为你寻找角色..."（3-5s）
    │
    ▼
一次 API 调用返回：
  · 5 个角色（name + matchScore + domain）
  · 5 份故事（人物介绍 + 多面人生片段）
    │
    ▼
气泡出现 + 故事已在内存中
    │
    ▼
用户点击任意气泡 → 瞬间显示故事浮窗 ✅
```

**技术实现：**
- 一次 LLM 调用，Prompt 要求返回 5 个角色 + 各自故事（结构化 JSON）
- 前端拿到结果后，角色信息用于渲染气泡，故事存入内存缓存
- 用户点击气泡直接从缓存读取，零等待
- 不需要单独的 `/api/story` 接口

### 功能 3：每日神秘卡

| 项目 | 说明 |
|------|------|
| 功能 | 每天有 1 张神秘卡混在气泡中，金色闪光外框 + 问号 |
| 展示形式 | 和普通气泡一起漂浮，不单独标识入口 |
| 交互 | 点击翻开 → 展示角色 + 故事浮窗 |
| 翻开后 | 变为普通气泡，可正常拖拽/收藏 |
| 刷新规则 | 每天一张，翻开后不可恢复 |

### 功能 4：故事浮窗

| 项目 | 说明 |
|------|------|
| 功能 | 点击气泡，弹出角色故事浮窗 |
| 内容 | 人物介绍 + 人物的主要故事，侧重体现**多面人生**主题 |
| 交互 | 关闭浮窗后点击其他气泡查看下一个角色 |
| 后续操作 | 点击"和 Ta 聊" → 进入 CBT 对话 |
| 视觉 | 背景模糊，其他气泡变暗 |

#### 浮窗内容结构

故事浮窗分两部分展示：

**第一部分：人物介绍**
- 角色是谁、来自哪个领域/作品
- 一句话标签（如"登月第一人"、"闹海少年"）
- 让用户快速了解这个角色的身份和标签

**第二部分：人物的主要故事（多面人生）**
- 不是单一维度的英雄叙事，而是展示角色人生的不同侧面
- 每个角色展示 2-3 个"人生片段"，体现角色的多面性
- 例如 Neil Armstrong：
  - 面A：登月时的冷静决断——燃料告急仍选择继续下降
  - 面B：回国后的低调——拒绝商业代言，回到小镇当教授
  - 面C：失去女儿的沉默——两岁女儿离世后，把悲伤埋进工作
- 主题：每个人都不只是一个标签，Ta 的人生比你以为的更丰富
- 目的：让用户在选角色之前，先看到角色作为"人"的多面性，而不是一个扁平的符号

### 功能 5：CBT 五模块对话

用户选中角色后，系统以角色口吻、按 CBT 逻辑输出五模块回答：

#### 模块 1：共情接纳

> 安抚情绪，先接住你的紧张，不评判，结合角色自身相似经历共情。

- **目标**：让用户感到被理解、被接纳
- **角色口吻**：用角色的语气和经历来共情，不是心理咨询师的模板话
- **示例（Neil Armstrong 回应"想辞职创业但很怕"）**：
  > "我理解你。1969 年，登月舱下降时燃料只剩不到 30 秒，警报声刺耳，地面说可能要中止任务。那一刻我也怕——怕失败，怕让所有人失望。你现在的紧张，我太熟悉了。"

#### 模块 2：揪出负面扭曲认知

> CBT 核心——找出脑子里的消极偏见，点破潜意识里的不合理想法。

- **目标**：帮用户识别自己的认知扭曲（灾难化、非黑即白、过度概括等）
- **方式**：角色直接点破，但用角色自己的风格
- **示例**：
  > "你说'辞职创业一定会失败'——这是你自己下的判决书，还没有发生就判了死刑。你脑子里在演一部灾难片，但主角不是你，是你编出来的恐惧。"

#### 模块 3：用角色经历反驳

> 拿角色遭遇过的同类困境举例，证明"最坏结果不一定发生，就算失败也有退路"。

- **目标**：用真实/虚构故事作为论据，破除内耗
- **方式**：角色讲述自己的经历，展示"我也面对过类似的事，结果并没有那么糟"
- **示例**：
  > "阿波罗 11 号着陆成功后，我们才发现导航计算机差点过载崩溃。如果我当时因为恐惧而中止，人类登月会推迟很多年。最坏的结果——失败——也没有想象中那么不可承受。我有退路，你也有。"

#### 模块 4：重构理性思维

> 替换消极想法为客观、正向的思考，给出全新的看待问题的角度。

- **目标**：帮用户建立新的认知框架
- **方式**：角色用自己的思维框架重新拆解问题
- **示例**：
  > "你不是在'辞职创业一定会失败'和'不辞职很安全'之间选。你是在'现在就迈出一步试试'和'永远不迈出但一直后悔'之间选。后者才是真正的失败——没有行动的遗憾比行动后的失败更难受。"

#### 模块 5：分步骤落地行动方案

> CBT 落脚点——可执行行为，杜绝空想。拆分具体怎么做、应急话术、应对失败的备选方案。

- **目标**：给出极小但有效的行动
- **原则**：简单到不可思议但实际有效
- **内容结构**：
  - **第一步（极小行动）**：今天就能做的、5 分钟内完成的事
  - **应急话术**：如果面对突发情况，准备一句什么样的话
  - **备选方案**：如果第一步失败/被拒绝，接下来怎么办
- **示例**：
  > **今天的第一步**：打开备忘录，写下你最想辞职创业的那 1 个理由。不是计划书，就 1 句话。
  > **应急话术**：如果有人问你"是不是要辞职"，就说"我正在认真想这件事"。
  > **备选方案**：如果写完发现理由不充分，说明现在不是辞职的时机——先在现有岗位上试一个小项目，验证你的想法。这不是放弃，是缩小风险。

#### 分步渲染机制

CBT 五模块**不一次性渲染全部内容**，而是按顺序逐一呈现给用户，**模块间自动衔接，无"继续"按钮**：

```
模块1 共情接纳 → 流式逐字输出 → 完成
    │
    │ 自动过渡（淡入动画，1-2秒间隔）
    ▼
模块2 揪出扭曲认知 → 流式逐字输出 → 完成
    │
    │ 自动过渡
    ▼
模块3 经历反驳 → 流式逐字输出 → 完成
    │
    │ 自动过渡
    ▼
模块4 重构理性思维 → 流式逐字输出 → 完成
    │
    │ 自动过渡
    ▼
模块5 落地行动方案 → 流式逐字输出 → 完成
    │
    │ 对话结束 → 记录到历史
    ▼
```

**设计原因：**
- 一次性输出五模块信息量大，用户容易跳读、失去注意力
- 按顺序逐个呈现让用户在每个模块停留、消化
- 自动衔接保持角色"连续说话"的感觉，不打断沉浸感
- 每个模块内部使用流式逐字输出，增强"角色正在说话"的感觉
- 模块之间有过渡动画（淡入/滑动），1-2 秒间隔

**技术实现：**
- LLM 一次生成五模块完整内容（结构化 JSON）
- 前端按模块顺序逐个展示，上一个模块流式输出完成后，自动开始下一个
- 无"继续"按钮，模块间自动衔接
- 模块之间有过渡动画（淡入/滑动）

### 功能 6：圆桌讨论

| 项目 | 说明 |
|------|------|
| 功能 | 用户拖拽 3 个气泡到右下角圆桌区，开启多角色讨论 |
| 交互 | 拖拽气泡 → 圆桌区（右下角半透明，满 3 人后亮起"开始"按钮） |
| 输出 | 3 个角色各给一个视角 + 一个故事，用户可从中选一个深入对话 |
| 目的 | 不同角色视角碰撞，帮用户照见内心倾向 |

### 功能 7：收藏

| 项目 | 说明 |
|------|------|
| 功能 | 长按气泡收藏角色 |
| 交互 | 长按 0.5 秒 → 气泡闪烁确认 → 底部收藏计数+1 |
| 入口 | 底部"收藏"按钮，点击展开横向列表 |
| 后续 | 收藏角色可直接点击对话，无需重新搜索 |

### 功能 8：历史记录

| 项目 | 说明 |
|------|------|
| 功能 | 记录用户历史对话 |
| 入口 | 底部"历史"按钮 |
| 展示 | 按时间倒序排列，显示角色 + 问题摘要 + 时间 |

---

## 三、用户流程

### 主流程

```
1. 用户打开应用
   → 气泡漂浮主界面 + 输入框，神秘卡混在气泡中

2. 用户输入问题
   → 示例："想提加薪但不敢开口"

3. 系统生成 5 个角色气泡 + 1 张神秘卡
   → 气泡大小 = 匹配度，颜色 = 领域

4. 用户操作气泡（三选一）：
   a. 点击气泡 → 故事浮窗 → 点击"和Ta聊" → CBT对话
   b. 拖拽气泡到圆桌区 → 满3人 → 点击"开始" → 圆桌讨论 → 选1人 → CBT对话
   c. 长按气泡 → 收藏

5. CBT 五模块对话（角色口吻）：
   模块1: 共情接纳 — 接住情绪，不评判
   模块2: 揪出扭曲认知 — 点破不合理想法
   模块3: 角色经历反驳 — 用故事破除内耗
   模块4: 重构理性思维 — 换个角度看问题
   模块5: 落地行动方案 — 极小行动+应急话术+备选方案

6. 对话结束
   → 记录到历史，气泡回到主界面
```

### 神秘卡流程

```
点击神秘卡气泡
  → 翻开动画（金色外框消散）
  → 展示角色 + 故事浮窗
  → 点击"和Ta聊" → CBT对话
  → 也可拖拽圆桌 / 长按收藏
```

### 圆桌流程

```
拖拽 3 个气泡到圆桌区
  → 圆桌满员，"开始"按钮亮起
  → 点击"开始"
  → 3 个角色各输出：一个视角 + 一个故事
  → 用户选 1 人
  → 进入 CBT 五模块对话
```

---

## 四、技术支持

### 技术架构

```
前端
  Web 应用（手机长宽比 375×812）
  技术栈：React / Next.js + Tailwind CSS

  模块：
  · 气泡引擎 — Canvas + 物理动画（速度/碰撞/反弹）
  · 拖拽系统 — Touch/Pointer Events + 圆桌碰撞检测
  · 故事浮窗 — Modal + 背景模糊
  · CBT对话 — 五模块逐个渲染 + 流式输出
  · 圆桌系统 — 多角色并行输出
  · 收藏/历史 — LocalStorage / 后端存储

后端 / AI 层
  LLM（大语言模型）

  功能：
  · 角色推荐：问题 → 5个匹配角色 + 匹配度分数
  · 角色画像：角色名 → 性格/风格/经历
  · 故事生成：角色+问题 → 相关经历
  · CBT五模块：角色+问题 → 五层结构化输出
  · 圆桌讨论：3角色+问题 → 各自视角
  · 每日神秘卡：日期 → 角色推荐

  数据层：
  · 用户对话历史
  · 收藏角色列表
  · 每日神秘卡记录
```

### CBT 五模块 Prompt 结构

```
系统 Prompt 框架：

你是 {角色名}，以你的性格、经历、说话风格回答用户的问题。

用户问题：{用户输入}

请按以下五模块结构回答，每个模块用 {角色名} 的第一人称口吻：

【模块1：共情接纳】
- 先接住用户的情绪，不评判
- 结合你自己的相似经历共情
- 2-3 句话

【模块2：揪出扭曲认知】
- 识别用户话语中的认知扭曲（灾难化/非黑即白/过度概括等）
- 直接点破，用你的风格
- 2-3 句话

【模块3：经历反驳】
- 讲述你经历过的同类困境
- 证明"最坏结果不一定发生，就算失败也有退路"
- 3-5 句话

【模块4：重构理性思维】
- 用你的思维框架重新拆解问题
- 给出客观、现实的新角度
- 2-3 句话

【模块5：落地行动】
- 第一步：今天能做的、5分钟内完成的极小行动
- 应急话术：面对突发情况准备的一句话
- 备选方案：第一步失败后的替代方案
```

### 关键技术点

| 模块 | 技术要点 |
|------|---------|
| 气泡物理引擎 | Canvas 渲染 + 简单物理模拟，可考虑 Matter.js 轻量版 |
| 拖拽系统 | Touch/Pointer Events，拖拽到圆桌区的碰撞检测 |
| 气泡大小匹配 | 输入问题 → LLM 分析 → 返回角色 + 匹配度分数 → 映射为大/中/小 |
| 气泡颜色映射 | 角色领域标签 → 预定义多巴胺渐变色值 |
| 神秘卡 | 每日生成 + LocalStorage 记录翻开状态 |
| CBT 五模块 | 结构化 Prompt + LLM 流式输出 + 前端逐模块渲染 |
| 圆桌讨论 | 3 次并行 LLM 调用（每个角色独立）或 1 次批量调用 |
| 收藏/历史 | 前期 LocalStorage，后续可升级为后端存储 |

### 性能要求

| 指标 | 要求 |
|------|------|
| 气泡动画帧率 | 60fps，不低于 30fps |
| 角色推荐响应 | < 3 秒 |
| 故事浮窗弹出 | < 200ms |
| CBT 对话首字输出 | < 2 秒（流式输出） |
| 圆桌讨论响应 | < 5 秒（3 角色并行） |

---

## 附录：界面状态总览

| 状态 | 展示 |
|------|------|
| 首次进入 | 气泡随机大小漂浮，神秘卡混在其中 |
| 输入问题后 | 气泡大小调整为匹配度，颜色对应领域 |
| 点击气泡 | 故事浮窗弹出，背景模糊 |
| 拖拽气泡 | 圆桌区高亮，松手气泡飞入 |
| 长按气泡 | 气泡闪烁，收藏计数+1 |
| 圆桌满 3 人 | 圆桌区发光，出现"开始"按钮 |
| 神秘卡翻开 | 金色外框消失，变为普通气泡 |
| CBT 对话中 | 五模块逐个渲染，流式输出 |
| 对话结束 | 记录到历史，气泡回到主界面 |

---

*Fake It PRD v2.0 · 2026-07-17 · CBT + 角色视角*
