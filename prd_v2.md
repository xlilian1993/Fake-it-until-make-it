# Fake It Until You Make It — PRD v2.0

> 2026-07-20 · 实际实现版本

---

## 一、产品定位

### 一句话定义

一款以"角色共鸣"为核心的轻量级心理陪伴工具。用户写下困惑，AI 推荐跨越时空的角色，通过一对一 CBT 五幕对话和三人圆桌讨论，帮用户获得新视角与行动启发。

**Slogan**: 总有一个灵魂，懂你的此刻。

### 核心理念

你遇到的问题，前人早就遇到过。不是 AI 给你建议，而是透过角色的眼睛看世界——借 Ta 的经历照见自己的路，借 Ta 的思维打破自己的惯性。

**Fake it until you make it。**

### 与传统 CBT 工具的区别

| 维度 | 传统 CBT 工具 | Fake It |
|------|-------------|---------|
| 语气 | 心理咨询师，专业冷静 | 你敬佩的角色，有温度有故事 |
| 动力来源 | 理性分析 | 角色经历 + 情感共鸣 |
| 接受度 | 需要用户主动学习 CBT | 用户无感知，自然完成 CBT 流程 |
| 好玩度 | 低，像做作业 | 高，像和偶像对话 |

---

## 二、用户流程

### 主流程

```
1. 用户打开应用 → 首页（诗意引导文案 + 输入框在下方）
2. 用户输入问题 + 提交
3. 加载页：进度条 + 随机角色卡片 → 最低展示 2s → "去看看"按钮
4. 气泡页：6 个角色气泡（1 神秘 + 5 普通）分布在全屏背景图 + 半透明白色卡片上
5. 用户操作气泡：
   a. 点击神秘气泡 → 翻转揭示 → 再次点击 → 故事浮窗
   b. 点击普通气泡 → 故事浮窗
   c. 拖拽普通气泡到圆桌区 → 凑满 3 人 → 开始讨论
6. 故事浮窗 → "和 Ta 聊" → CBT 五幕对话
7. CBT 对话结束 → 分享长图 / 回到气泡
8. 圆桌讨论 → 链式接力发言 → 选一人深入对话
```

---

## 三、页面状态详解

### 状态 1：首页（空态）

- **顶部**：标题 "Fake It Until You Make It"（渐变金色文字）+ 副标题引用
- **中部**：诗意引导文案，引用苏轼、三毛、贝多芬、梵高等意象
- **底部**：输入框 + 提交按钮（→）
- **输入框**：占位词 3 秒轮播（3 条预设问题）
- **输入限制**：最大 200 字
- **提交方式**：Enter 键或点击按钮
- **背景**：全屏 avatars-bg.png

### 状态 2：加载页（Loading）

- **进度条**：0% → 85%（模拟增长，250ms 间隔，分段速度），完成后跳至 100%
- **引导语**："把心事写下来，时光另一头，自有人回信。"（居中 italic）
- **角色卡片**：从新角色池（81 人）中随机选取，展示头像 + 名字 + 标签 + 引用语 + 生平描述
- **卡片样式**：半透明白色背景 `rgba(255,255,255,0.5)`，圆角阴影
- **完成按钮**："去看看"（粉色 pill 按钮）
- **最低展示时间**：2 秒（minDisplayTimer），确保用户看到加载过程
- **背景**：全屏 avatars-bg.png

### 状态 3：气泡页（推荐结果）

- **背景**：全屏 avatars-bg.png
- **卡片区域**：白色半透明卡片 `rgba(255, 248, 243, 0.6)` + 虚线边框 `rgba(139,134,128,0.5)`，位于上方约 56% 高度
- **顶部**：返回主页按钮（←） + 用户问题引用显示
- **6 个气泡**：
  - 1 个"今日限定"神秘气泡：居中偏上（cx:0.50, cy:0.42），金色发光
  - 5 个普通气泡：五区域分散（左上/右上/左下/右下/底部中央）
  - 气泡大小按匹配度三档：large(≥80)/medium(≥60)/small(<60) → 80/60/44px
  - CSS 浮动动画 + 径向渐变背景 + 白色高光
  - 每个气泡上方显示角色名标签
- **圆桌区**：右下角 130×130px 圆形区域（见状态 6）
- **引用文案**（圆桌未激活时）：卡片下方居中显示 "你走在自己的夜里，而有人曾提灯走过同一段路。"
- **引导文字**：顶部 "点击气泡跟TA聊聊" 或 "点击气泡跟TA聊聊，或者拖进圆桌群聊"

### 状态 4：神秘气泡

- **外观**：金色渐变 + 发光 boxShadow + 神秘面孔图片
- **标签**："✨ 今日限定"
- **交互**：首次点击 → 翻转动画 0.5s → 揭示真实角色头像 → 标签变为角色名
- **再次点击**：打开故事浮窗
- **来源**：每日从三人池（泰勒斯威夫特/哈兰德/甄嬛）随机抽取
- **不可拖入圆桌**

### 状态 5：故事浮窗（Story Modal）

- **动画**：从底部 spring 弹入，顶部色条（按角色领域配色）
- **内容**：
  - 头像 + 姓名 + 来源 + 一句标签（tagline，≤10 字）
  - "多面人生"：3 个生命片段（至暗时刻 / 转折顿悟 / 启示），带左侧色条装饰
  - 片段为第一人称口吻，LLM 生成时要求贴合用户问题
- **操作按钮**：
  - "💬 和 Ta 聊" → 进入 CBT 对话（粉色渐变按钮）
  - "👥 拉Ta群聊" → 加入圆桌（粉色浅底文字按钮）
- **背景**：黑色半透明 + backdrop-blur

### 状态 6：CBT 一对一对话

- **进入方式**：全屏弹层（spring 动画从底部滑入），背景色 #EDEDED
- **顶部导航栏**：返回按钮 + 角色名居中
- **步骤指示器**：
  - 横向排列的 pill 形状（圆角矩形）
  - 颜色状态：past（半透明域色）/ current（实色域色，scale 1.05）/ future（极淡域色）
  - 每个 pill 显示短标题（如"你的处境"、"隐藏的台词"），箭头连接
  - 上方显示场景编号（如"第一幕"、"第二幕"）
- **引语**："你此刻的心情，千百年前就有人想好了答案。"
- **五幕结构**：
  1. **第一幕：你的处境** — 帮用户看清现状
  2. **第二幕：隐藏的台词** — 指出自动负面念头
  3. **第三幕：换个剧本** — 角色经历给出新视角
  4. **第四幕：新的对白** — 替代性内心对话
  5. **第五幕：你的舞台** — 行动建议（含 3 个子卡片）
- **打字机动画**：每幕逐字输出（20ms/字），带闪烁光标
- **推进方式**：点击屏幕空白处推进到下一幕
- **衔接提示**：当前幕完成后显示 "点击屏幕继续"（闪烁动画）
- **第五幕特殊处理**：展开三个行动卡片（🎬 今天的排练 / 🎭 新台词 / 🪄 进阶剧本），用域色浅背景
- **加载状态**：顶部 shimmer 进度条
- **完成状态**：展示操作按钮行（回到气泡 / 📤 分享长图 / 🔍 深入聊聊 / ❤️ / 👎）
- **防御处理**：第五幕缺少 action 数据时用 content 兜底

### 状态 7：圆桌讨论（Round Table）

- **圆桌区（未激活）**：右下角 130×130px，虚线圆环，中心"群聊"标签 + 人数计数 "0/3"
- **拖拽加入**：
  - 从普通气泡 pointerdown → pointermove → pointerup
  - 移动时显示幽灵气泡（跟随手指）
  - 进入圆桌区时幽灵气泡边框高亮 + 发光
  - 松手时若在圆桌区内 → 加入成员列表
  - 最多 3 人，超出时移除最早加入的（shift）
- **成员排列**：三角布局（三个角度：-90°/30°/150°），距圆心 40px
- **成员头像**：圆形，域色渐变背景，白色边框，点击可移出
- **"开始讨论"按钮**：满 3 人时出现，粉色渐变，带 pulse-soft 动画
- **链式发言**（3 次串行 API 调用）：
  1. 第一人独立发言
  2. 第二人基于第一人观点回应（传入 previous）
  3. 第三人基于第二人观点回应（传入 previous）
- **加载文案**："XX 正在抢麦..." → "XX 正在接话 XX..."
- **结果浮窗（RoundTableResult）**：
  - 从底部弹出，背景模糊
  - 三位角色依次展示：头像 + 姓名 + 第 N 位发言 + 💡 观点 + 📖 经历
  - 每张卡片使用角色域色浅背景
  - Loading 中显示..." pulse-soft 动画
  - 全部完成后：选一人深入对话（3 个按钮）+ 📤 分享长图

### 状态 8：分享长图

- **技术方案**：html2canvas 渲染 DOM → Canvas → Blob URL / Data URL
- **卡片设计**：340px 宽，#FDF6EE 暖色背景，圆角 16px，Noto Serif SC 字体
- **CBT 对话分享内容**：
  - 标题："与 XX 的对话"
  - 用户问题 + 五幕内容 + 行动卡片
  - 底部："— Fake it until you make it —" + 二维码
- **圆桌分享内容**：
  - 标题："圆桌讨论"
  - 讨论话题 + 三人观点 + 经历
  - 底部二维码
- **PC 端**：自动下载 .png 文件
- **移动端**：展示浮层（黑色半透明背景），提示"长按图片保存到相册"，支持关闭
- **微信浏览器**：使用 Blob URL 替代 Data URL（规避微信对 data URL 的大小限制）
- **二维码**：https://xlilian.cn/fake-it 入口，60×60px

---

## 四、角色系统

### 角色池

| 池 | 文件 | 数量 | 主题 |
|----|------|------|------|
| 原始池 | `characters.ts` | 47 | 热门角色（历史/影视/动漫/游戏/书籍） |
| 新角色池 | `characters_new.ts` | 81 | 跨界反差 / 她力量 / 平凡微光 / 大器晚成 / 逆风翻盘 / 一生一事 / 少年无惧 / 残缺超越 |

每个新角色有：姓名、标签(tagline)、引用语(quote)、生平描述(desc)、PNG 头像文件名

### 热度分值

- 每个角色有热度分值（0-100），用于 LLM 综合排序
- 综合评分 = 问题关联度(60%) + 角色热度(40%)
- 原始池热度定义在 `CHARACTER_HOTNESS`，新池定义在 `NEW_CHARACTER_HOTNESS`
- 未配置默认 50 分

### 领域分类与颜色

| 领域 | 颜色值 | 含义 |
|------|--------|------|
| leader | #FFD700 金 | 领袖/创业者 |
| philosopher | #B8A9FF 紫 | 哲人/思想家 |
| explorer | #7FE5FF 青 | 探索者/科学家 |
| healer | #A8FFD7 绿 | 治愈者/导师 |
| rebel | #FF9EC7 粉 | 反叛者/战士 |
| creator | #FFD49E 橙 | 创作者/艺术家 |

### 头像系统

- 两级映射：`AVATAR_MAP`（原始池）+ `NEW_AVATAR_MAP`（新角色池）
- `getAvatar(name)` 先查原始池，再查新池，都没找到返回 '🫧'
- `isImageAvatar(avatar)` 判断是否包含 '/avatars/' 路径
- 苏东坡 → 苏轼别名映射

---

## 五、LLM Prompt 设计

### 推荐 Prompt（buildRecommendPrompt）
- 传入用户问题 + 今日限定角色名
- 返回 6 个角色（1 神秘 + 5 普通）
- 角色从角色池中严格选取（禁止自创）
- 匹配度 95-65 递减
- 每个角色 3 个生命片段（第一人称）

### CBT Prompt（buildCBTPrompt）
- 角色第一人称扮演
- 固定五幕 JSON 格式
- 第五幕含 action 三个子字段

### 圆桌 Prompt（buildRoundTablePrompt）
- 第一人独立发言
- 后续基于上一位观点回应 + 自述
- JSON 格式，包含 viewpoint + story

---

## 六、非功能需求

| 项目 | 说明 |
|------|------|
| 移动端优先 | 375-414px 基准，响应式适配 |
| 微信兼容 | Blob URL 分享 |
| LLM 超时 | 推荐/CBT 30s，圆桌 60s |
| 输入限制 | 200 字 |
| 最低加载时间 | 2 秒 |
| loading 进度模拟 | 250ms 间隔，分阶段速度（0-20%: 2.5, 20-50%: 1.8, 50-85%: 1.0） |
| API JSON 容错 | 自动去 markdown 代码块 + 尾逗号修复 + 控制字符清除 |

---

*PRD v2.0 · 2026-07-20 · 实际实现版本*
